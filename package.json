{
  "name": "chatty",
  "version": "1.0.0",
  "description": "Combined package.json for frontend and backend",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix vite-project && npm run build --prefix vite-project",
    "start": "npm start --prefix backend", 
    "dev": "npm run dev:backend & npm run dev:frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix vite-project"
  },
  "dependencies": {},
  "devDependencies": {},
  "keywords": [],
  "author": "",
  "license": "ISC"
}
